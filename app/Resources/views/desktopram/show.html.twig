{% extends 'base.html.twig' %}

{% block body %}
    <h1>Desktopram</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ desktopRam.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ desktopRam.name }}</td>
            </tr>
            <tr>
                <th>Model</th>
                <td>{{ desktopRam.model }}</td>
            </tr>
            <tr>
                <th>Series</th>
                <td>{{ desktopRam.series }}</td>
            </tr>
            <tr>
                <th>Brand</th>
                <td>{{ desktopRam.brand }}</td>
            </tr>
            <tr>
                <th>Capacity</th>
                <td>{{ desktopRam.capacity }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ desktopRam.type }}</td>
            </tr>
            <tr>
                <th>Busspeed</th>
                <td>{{ desktopRam.busSpeed }}</td>
            </tr>
            <tr>
                <th>Caslatency</th>
                <td>{{ desktopRam.casLatency }}</td>
            </tr>
            <tr>
                <th>Numberofpins</th>
                <td>{{ desktopRam.numberOfPins }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('desktopram_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('desktopram_edit', { 'id': desktopRam.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}

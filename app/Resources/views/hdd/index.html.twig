{% extends 'base.html.twig' %}

{% block title %} Dyski twarde HDD {% endblock %}

{% block mainbar %}
    <div class="main-bar">
        <div>
            <a href="{{ path('hdd_index') }}" class="btn btn-metis-5 btn-grad btn-rect btn-md">
                <i class="fa fa-arrow-down"></i>
                <span class="link-title">Lista dysków</span>
            </a>
            <a href="{{ path('hdd_new') }}" class="btn btn-metis-5 btn-grad btn-rect btn-md">
                <i class="fa fa-plus"></i>
                <span class="link-title">Dodaj</span>
            </a>
        </div>
    </div>
    <!-- /.main-bar -->
{% endblock %}


{% block body %}

    <div id="content">
        <div class="outer">
            <div class="inner bg-light lter">
                <!--Begin Datatables-->
                <div class="row">
                    <div class="col-lg-12">
                        <div class="box">
                            <header>
                                <div class="icons"><i class="fa fa-desktop"></i></div>
                                <h5>Lista dysków</h5>
                            </header>
                            <div id="collapse4" class="body">
                                <table id="dataTable"
                                       class="table table-bordered table-condensed table-hover table-striped">
                                    <thead>
                                    <tr>
                                        <th>Nazwa</th>
                                        <th>Model</th>
                                        <th>Seria</th>
                                        <th>Producent</th>
                                        <th>Pojemność GB</th>
                                        <th>Format</th>
                                        <th>Interfejs</th>
                                        <th>Prędkość obrotowa rpm</th>
                                        <th>Cache MB</th>
                                    </tr>
                                    </thead>
                                    <tbody>
                                    {% for hdd in hdds %}
                                        <tr onclick='location.href = "{{ path('hdd_show', { 'id': hdd.id }) }}"'>
                                            <td>{{ hdd.name }}</td>
                                            <td>{{ hdd.model }}</td>
                                            <td>{{ hdd.series }}</td>
                                            <td>{{ hdd.brand }}</td>
                                            <td>{{ hdd.capacity }}</td>
                                            <td>{{ hdd.formFactor }}</td>
                                            <td>{{ hdd.interface }}</td>
                                            <td>{{ hdd.rotationSpeedRpm }}</td>
                                            <td>{{ hdd.cache }}</td>
                                        </tr>
                                    {% endfor %}
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                </div>
                <!-- /.row -->
                <!--End Datatables-->

            </div>
            <!-- /.inner -->
        </div>
        <!-- /.outer -->
    </div>
    <!-- /#content -->
{% endblock %}




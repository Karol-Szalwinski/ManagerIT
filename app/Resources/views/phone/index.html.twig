{% extends 'base.html.twig' %}

{% block title %} Telefony {% endblock %}

{% block mainbar %}
    <div class="main-bar">
        <div>
            <a href="{{ path('phone_index') }}" class="btn btn-metis-5 btn-grad btn-rect btn-lg">
                <i class="fa fa-arrow-down"></i>
                <span class="link-title">Lista telefonów</span>
            </a>
            <a href="{{ path('phone_new') }}" class="btn btn-metis-5 btn-grad btn-rect btn-lg">
                <i class="fa fa-plus"></i>
                <span class="link-title">Dodaj</span>
            </a>
        </div>
    </div>
    <!-- /.main-bar -->
{% endblock %}

{% block body %}

    <div id="content">
        <div class="outer">
            <div class="inner bg-light lter">
                <!--Begin Datatables-->
                <div class="row">
                    <div class="col-lg-12">
                        <div class="box">
                            <header>
                                <div class="icons"><i class="fa fa-file-text"></i></div>
                                <h5>Lista telefonów</h5>
                            </header>

                            <div id="collapse4" class="body">

                                <table id="dataTable"
                                       class="table table-bordered table-condensed table-hover table-striped">
                                    <thead>
                                    <tr>
                                        <th>Nazwa</th>
                                        <th>Marka</th>
                                        <th>Model</th>
                                        <th>S/N</th>
                                        <th>Modem</th>
                                        <th>Przekątna ekranu</th>
                                        <th>Kolor</th>
                                        <th>Opis</th>
                                        <th>Sim</th>
                                        <th>Użytkownik</th>
                                    </tr>
                                    </thead>
                                    <tbody>
                                    {% for phone in phones %}
                                        <tr onclick='location.href = "{{ path('phone_show', { 'id': phone.id }) }}"'>
                                            <td>{{ phone.name }}</td>
                                            <td>{{ phone.brand }}</td>
                                            <td>{{ phone.model }}</td>
                                            <td>{{ phone.serial }}</td>
                                            <td>{{ phone.modem }}</td>
                                            <td>{{ phone.screenSize }}</td>
                                            <td>{{ phone.color }}</td>
                                            <td>{{ phone.description }}</td>
                                            <td>{{ phone.sim }}</td>
                                            <td>{{ phone.employee }}</td>
                                        </tr>
                                    {% endfor %}
                                    </tbody>
                                </table>
                            </div>

                        </div>
                    </div>
                </div>
                <!-- /.row -->
                <!--End Datatables-->
            </div>
            <!-- /.inner -->
        </div>
        <!-- /.outer -->
    </div>
    <!-- /#content -->
{% endblock %}
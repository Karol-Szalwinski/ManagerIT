{% extends 'base.html.twig' %}

{% block title %} Dyski twarde SDD {% endblock %}

{% block mainbar %}
    <div class="main-bar">
        <div>
            <a href="{{ path('sdd_index') }}" class="btn btn-metis-5 btn-grad btn-rect btn-lg">
                <i class="fa fa-arrow-down"></i>
                <span class="link-title">Lista dysków</span>
            </a>
            <a href="{{ path('sdd_new') }}" class="btn btn-metis-5 btn-grad btn-rect btn-lg">
                <i class="fa fa-plus"></i>
                <span class="link-title">Dodaj</span>
            </a>
        </div>
    </div>
    <!-- /.main-bar -->
{% endblock %}


{% block body %}

    <div id="content">
        <div class="outer">
            <div class="inner bg-light lter">
                <!--Begin Datatables-->
                <div class="row">
                    <div class="col-lg-12">
                        <div class="box">
                            <header>
                                <div class="icons"><i class="fa fa-desktop"></i></div>
                                <h5>Lista dysków</h5>
                            </header>
                            <div id="collapse4" class="body">
                                <table id="dataTable"
                                       class="table table-bordered table-condensed table-hover table-striped">
                                    <thead>
                                    <tr>
                                        <th>Nazwa</th>
                                        <th>Model</th>
                                        <th>Seria</th>
                                        <th>Producent</th>
                                        <th>Pojemność GB</th>
                                        <th>Format</th>
                                        <th>Interfejs</th>
                                    </thead>
                                    <tbody>
                                    {% for sdd in sdds %}
                                        <tr onclick='location.href = "{{ path('sdd_show', { 'id': sdd.id }) }}"'>
                                            <td>{{ sdd.name }}</td>
                                            <td>{{ sdd.model }}</td>
                                            <td>{{ sdd.series }}</td>
                                            <td>{{ sdd.brand }}</td>
                                            <td>{{ sdd.capacity }}</td>
                                            <td>{{ sdd.formFactor }}</td>
                                            <td>{{ sdd.interface }}</td>
                                        </tr>
                                    {% endfor %}
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                </div>
                <!-- /.row -->
                <!--End Datatables-->

            </div>
            <!-- /.inner -->
        </div>
        <!-- /.outer -->
    </div>
    <!-- /#content -->
{% endblock %}



